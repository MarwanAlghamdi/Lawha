/** LAWHA: every geometry and palette constant the emitter uses, in one place. */

/** Matches `tableElement.ts`'s own constant, so measurement agrees with drawing. */
export const CELL_PADDING = 6;

/** One line of cell text plus its padding, at the default cell font size. */
export const TABLE_ROW_HEIGHT = 30;

/** Excalidraw's roundness discriminators. */
export const ROUNDNESS_PROPORTIONAL = 2;
export const ROUNDNESS_ADAPTIVE = 3;

/** Left where Excalidraw's own defaults are, so a converted diagram looks native. */
export const DEFAULT_STROKE = "#1e1e1e";
export const DEFAULT_BACKGROUND = "transparent";
